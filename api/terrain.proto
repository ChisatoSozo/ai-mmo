syntax = "proto3";
package terrain;

import "common.proto";

message TerrainChunk {
    common.Chunk chunk = 1;
    int32 width = 2;
    int32 length = 3;
    int32 height = 4;
    bytes data = 5;
}

service Terrain {
    rpc Get(stream common.Chunk) returns (stream TerrainChunk) {};
}