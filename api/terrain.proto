syntax = "proto3";
package terrain;

import "common.proto";

message TerrainChunk {
    int32 width = 1;
    int32 length = 2;
    int32 height_divisor = 3;
    bytes data = 4;
}

service Terrain {
    rpc Get(common.Chunk) returns (TerrainChunk) {};
}